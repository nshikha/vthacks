<script src="/socket.io/socket.io.js"></script>
<script src="/javascripts/jquery-v2.1.1-beta1.js"></script>
<script src="/javascripts/keyboard.js"></script>
<canvas id="userCanvas" style="display:none; border:1px solid #000000;">
    no canvas
</canvas>
<meta name=viewport
  content="width=device-width, user-scalable=no">
<meta name="apple-mobile-web-app-capable" content="yes">
<script src="/config.js"></script>
<script src="/javascripts/Snake.js"></script>
<script src="/javascripts/Controller.js"></script>
<script>
$(document).ready(function() {
  window.onerror = function(message, url, linenumber) {
  alert("Javascript error: " + message+ " on line " + linenumber + " for " + url);
  }
  var socket = io.connect('/', {reconnect: false});
  socket.on('init', function (data) {
    if (data === 'snake') {
        startSnake(socket);
    } else {
        startController(socket);
    }
  });

  socket.on('disconnect', function(){
      alert('Looks like the server died. Refresh when it goes back up.');
  });
});

</script>
<audio id="sound">
  <source src="gucci_4.mp3"></source>
</audio>
<audio id="snakesound">
  <source src="biggie_2.mp3"></source>
</audio>

